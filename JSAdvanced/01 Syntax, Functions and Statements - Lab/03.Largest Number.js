function lagestNumber(...numbers){
    let maxNumber = Math.max(...numbers);
    //console.log('The largest number is '+maxNumber+'.');
    console.log(`The largest number is ${maxNumber}.`);
}

lagestNumber(5,-3,16);